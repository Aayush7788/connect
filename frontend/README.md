# Frontend

Contains Flutter Android app and Next.js admin dashboard.

## Mobile

The Android app will live under `frontend/mobile` and use `frontend/mobile/.env.example` as its environment contract until the Flutter project is generated.

## Admin

The admin dashboard will live under `frontend/admin` and use `frontend/admin/.env.example` as its public environment contract until the Next.js project is generated.
