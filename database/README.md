# Database

PostgreSQL schema, Alembic migrations, seed data, RLS notes, and SQL indexes.

Database work starts in Phase 5. Keep migration files under `database/migrations` and seed data under `database/seeds`.
